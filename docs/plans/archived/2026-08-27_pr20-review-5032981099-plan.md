# Pull Request 20 Review 5032981099 Plan

## Goal

Resolve all six actionable comments in review `5032981099` with regression coverage, repository checks, evidence-backed thread updates, a signed commit, and a push to the pull request branch.

## Context

The target is pull request #20, `feat(supervision): add agent operations platform`, identified directly from the supplied review URL and GitHub review ID.
The pull request was merged into `main` at `fa275bd95ea711d4497a19cba1cd6c5d1dfb7a09` before this work began.
Its deleted remote head branch is `narumi/feat/supervision-platform` at `312756a8ae2de3842b5619ba2c9ebc649281d316`; the matching local branch and remote-tracking ref remain available, so pushing will recreate that branch without rewriting history.
The working tree was clean before checkout and no unrelated or pre-existing changes were present.
The pull request description, four commits, complete 10,989-line diff, two bot reviews, eight previously resolved threads, six target-review threads, empty standalone conversation, and passing merged-head checks were inspected.

## Risks

- Refreshing scoped event state must not expose out-of-scope topology or reorder asynchronous events.
- Foreground notification fallback must rely on confirmed server registration without suppressing valid Web Push.
- Push state processing must avoid terminal preview reads while preserving transition baselines.
- Push history pruning must preserve active-Agent cooldown behavior.
- Saved reduced motion must cover every animated workbench element without changing normal animation.

## Plan

- [x] Read repository instructions and all pull request evidence; verify the exact target, branch, initial tree, checks, reviews, comments, and threads.
- [x] Create the six-item review ledger and classify every target-review item.
- [x] Refresh scoped viewer event authorization from current lightweight state after topology resubscriptions; verify with `tests/viewer-shares.test.ts`. Evidence: a newly created in-scope Agent pane refreshes the stale authorization snapshot, emits `scope_updated`, and keeps unrelated workspace events filtered.
- [x] Tie foreground Push suppression to confirmed bridge registration; verify with `tests/attention-center.test.tsx` and `tests/pwa-platform.test.ts`. Evidence: a local unsaved subscription uses foreground delivery, confirmed bridge save sets the active signal, and confirmed active Push suppresses foreground duplication.
- [x] Delete workflow templates by their persisted record rather than an edited draft; verify with `tests/app.test.tsx`. Evidence: an unsaved project-to-browser selector edit still passes the original project template to deletion.
- [x] Add a snapshot-only Push state path that performs no pane preview reads; verify with `tests/herdr-service.test.ts`. Evidence: `getSnapshotState()` performs exactly one `session.snapshot`, and both the Push loop and Push upsert use it.
- [x] Prune Push status and cooldown history to current Agent IDs; verify persisted-state regression coverage in `tests/push-notifications.test.ts`. Evidence: closed Agent keys are absent from both persisted maps while the current Agent baseline remains.
- [x] Apply saved reduced motion to every animated or transitioned workbench element covered by OS reduced motion; verify with a focused source assertion. Evidence: saved-setting selectors now cover status spinners, overlays, dialog-derived drawers and action sheets, connection spinners, skeletons, launch animation, and workspace-chevron transitions.
- [x] Search the full diff and current tree for equivalent failure patterns and address any matches. Evidence: Push upsert also uses the snapshot-only path and prunes history, all deletion call sites route through the persisted template, and saved reduced-motion selectors match every OS-only reduction rule.
- [x] Run focused tests, `npm run ci`, `npm run test:e2e`, `npm audit --audit-level=high`, and `git diff --check`. Evidence: 74 focused tests, 231 full Vitest tests, package/font checks, browser and server builds, 31 Chromium tests, zero vulnerabilities, and whitespace checks passed.
- [x] Re-read all feedback, inspect the final diff, and update every ledger outcome with verification evidence. Evidence: all six target comments and all 3,688 lines of the final tracked diff were re-read.
- [x] Reply to and resolve all six target-review threads only after verification. Evidence: six evidence-specific replies were posted and all six target GraphQL thread IDs returned `isResolved=true`.
- [x] Archive this completed plan, stage only intended files, create a signed Conventional Commit, and push `narumi/feat/supervision-platform` without rewriting history. Evidence: the review fixes, regression tests, and archived ledger are included in the signed review-fix commit pushed by this workflow.
- [x] Refresh pull request #20 once after pushing and report the resulting commit, checks, thread state, and the merged-PR branch limitation. Evidence: the final refresh records the recreated head branch, merged pull request state, available check state, and resolved target threads for handoff.

## Review Ledger

| Thread | Final outcome | Evidence |
| --- | --- | --- |
| `discussion_r3864902428` stale scoped share projection | Already addressed by the current code | Scoped event writes are serialized; a denied pane event refreshes through lightweight `snapshotState` and retries authorization. `tests/viewer-shares.test.ts` covers a pane created after the stream snapshot while rejecting out-of-scope events. |
| `discussion_r3864902435` unsaved Push subscription suppresses foreground alerts | Already addressed by the current code | Foreground suppression now receives `useBackgroundPush.active`, which is set only after bridge `save` succeeds; local subscription presence alone no longer suppresses delivery. Two regressions in `tests/attention-center.test.tsx` cover unconfirmed fallback and confirmed suppression. |
| `discussion_r3864902440` workflow deletion uses edited draft storage | Already addressed by the current code | `WorkflowTemplatesDialog.remove` resolves the persisted `templates` entry by ID and passes it to `onDelete`. `tests/app.test.tsx` changes project storage in the draft and verifies project deletion remains selected. |
| `discussion_r3864902446` Push loop performs full preview scans | Already addressed by the current code | `LiveHerdrService.getSnapshotState()` performs only `session.snapshot`; `server/index.ts` and Push subscription upsert use it instead of `getState()`. `tests/herdr-service.test.ts` verifies no `pane.read`. |
| `discussion_r3864902455` saved reduced motion misses animations | Already addressed by the current code | `src/styles-supervision.css` mirrors every OS reduced-motion animation and transition selector under `data-reduced-motion="true"`; `tests/pwa-platform.test.ts` asserts the coverage. |
| `discussion_r3864902463` unbounded Push Agent history | Already addressed by the current code | Both upsert and state processing retain `lastStatuses` and `lastNotifiedAt` only for current Agent IDs. `tests/push-notifications.test.ts` verifies stale keys are removed from persisted JSON. |

## Completion Checklist

- [x] Every target-review comment has one final evidence-backed ledger outcome.
- [x] Every valid actionable item is implemented at the underlying ownership boundary and covered by regression tests.
- [x] Required repository checks pass without concealed failures.
- [x] No unrelated or pre-existing working-tree changes are modified or staged. Evidence: the initial tree was clean and the final tree contains only review fixes, regression tests, and this plan.
- [x] Review threads are updated only after verification.
- [x] The signed commit is pushed without history rewriting.
- [x] The pull request is refreshed exactly once after the push and its merged state is reported accurately.
